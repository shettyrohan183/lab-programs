#include<stdio.h>
#include<conio.h>
#include<time.h>
#include<math.h>

heapify(int n,int a[])
{
    int i,j,k,heap;
    
}

void main(){
    int n,i=0,j=0;
    clock_t start,end;
    double totaltime;
    printf("enter the number of elements :");
    scanf("%d ",&n);
    int a[n];
    printf("enter the elements \n");
    for(i=0;i<n;i++)
{
    scanf("%d",&a[i]);

}
start=clock();
heapify(n,a);

}